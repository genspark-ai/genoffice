import { readFileSync, writeFileSync } from 'node:fs'

function patch(path, from, to) {
  const src = readFileSync(path, 'utf8')
  if (src.includes(to)) return
  if (!src.includes(from)) throw new Error(`Patch anchor not found in ${path}`)
  writeFileSync(path, src.replace(from, to))
}

// Stop the shared AI IPC from forcing every saved configuration back to Genspark.
patch('apps/docs/src/main/docs-main.ts', `    // AI features all go through Genspark (gsk login); legacy settings with another provider are reset\n    settings.provider = 'genspark'\n    return settings`, `    return settings`)

// Home API: expose the shared ai-settings.json used by the editor AI panels.
patch('apps/shell/src/shared/home-api.ts', `  /** app version (from package.json / electron app.getVersion) */\n  getAppVersion(): Promise<string>`, `  /** OpenRouter/Nemotron settings shared by all editor AI panels */\n  getOpenRouterSettings(): Promise<OpenRouterSettings>\n  setOpenRouterSettings(settings: OpenRouterSettings): Promise<void>\n  /** app version (from package.json / electron app.getVersion) */\n  getAppVersion(): Promise<string>`)
patch('apps/shell/src/shared/home-api.ts', `export interface AccountStatus {`, `export interface OpenRouterSettings {\n  apiKey: string\n  model: string\n}\n\nexport interface AccountStatus {`)
patch('apps/shell/src/shared/home-api.ts', `  accountLogout: 'home:account-logout',\n  getAppVersion: 'home:get-app-version',`, `  accountLogout: 'home:account-logout',\n  getOpenRouterSettings: 'home:get-openrouter-settings',\n  setOpenRouterSettings: 'home:set-openrouter-settings',\n  getAppVersion: 'home:get-app-version',`)

// Preload bridge.
patch('apps/shell/src/preload/index.ts', `  async getAppVersion() {`, `  async getOpenRouterSettings() {\n    const result: unknown = await ipcRenderer.invoke(HOME_CHANNELS.getOpenRouterSettings)\n    const value = result as { apiKey?: unknown; model?: unknown } | null\n    return { apiKey: typeof value?.apiKey === 'string' ? value.apiKey : '', model: typeof value?.model === 'string' ? value.model : 'nvidia/nemotron-3-ultra-550b-a55b:free' }\n  },\n  async setOpenRouterSettings(settings) { await ipcRenderer.invoke(HOME_CHANNELS.setOpenRouterSettings, settings) },\n  async getAppVersion() {`)

// Main-process persistence. This writes the ai-settings.json consumed by registerAiIpc().
patch('apps/shell/src/main/index.ts', `  ipcMain.handle(HOME_CHANNELS.getAppVersion, (): string => app.getVersion())`, `  ipcMain.handle(HOME_CHANNELS.getOpenRouterSettings, () => {\n    const path = join(app.getPath('userData'), 'ai-settings.json')\n    try {\n      const raw = JSON.parse(readFileSync(path, 'utf8')) as { providers?: Record<string, { apiKey?: string; model?: string }> }\n      const cfg = raw.providers?.openrouter\n      return { apiKey: cfg?.apiKey ?? '', model: cfg?.model ?? 'nvidia/nemotron-3-ultra-550b-a55b:free' }\n    } catch { return { apiKey: '', model: 'nvidia/nemotron-3-ultra-550b-a55b:free' } }\n  })\n  ipcMain.handle(HOME_CHANNELS.setOpenRouterSettings, (_event, value: unknown) => {\n    const input = value as { apiKey?: unknown; model?: unknown } | null\n    const apiKey = typeof input?.apiKey === 'string' ? input.apiKey.trim() : ''\n    const model = typeof input?.model === 'string' && input.model.trim() ? input.model.trim() : 'nvidia/nemotron-3-ultra-550b-a55b:free'\n    const path = join(app.getPath('userData'), 'ai-settings.json')\n    let saved: Record<string, unknown> = {}\n    try { saved = JSON.parse(readFileSync(path, 'utf8')) as Record<string, unknown> } catch { /* first run */ }\n    const providers = (saved.providers && typeof saved.providers === 'object' ? saved.providers : {}) as Record<string, unknown>\n    providers.openrouter = { apiKey, model }\n    saved.provider = 'openrouter'\n    saved.providers = providers\n    writeFileSync(path, JSON.stringify(saved, null, 2))\n  })\n  ipcMain.handle(HOME_CHANNELS.getAppVersion, (): string => app.getVersion())`)

// Turn the old account control into a direct AI Settings control. No Genspark login is shown.
patch('apps/shell/src/renderer/src/Home.tsx', `  const [appVersion, setAppVersion] = useState('')`, `  const [appVersion, setAppVersion] = useState('')\n  const [showAiSettings, setShowAiSettings] = useState(false)\n  const [openRouterKey, setOpenRouterKey] = useState('')\n  const [openRouterModel, setOpenRouterModel] = useState('nvidia/nemotron-3-ultra-550b-a55b:free')\n  const [aiSaved, setAiSaved] = useState(false)`)
patch('apps/shell/src/renderer/src/Home.tsx', `    void window.aiOffice.getAppVersion?.().then((v) => {\n      if (alive && v) setAppVersion(v)\n    })`, `    void window.aiOffice.getAppVersion?.().then((v) => { if (alive && v) setAppVersion(v) })\n    void window.aiOffice.getOpenRouterSettings?.().then((cfg) => { if (alive) { setOpenRouterKey(cfg.apiKey); setOpenRouterModel(cfg.model) } })`)

// Replace the account menu's login section with OpenRouter configuration access.
patch('apps/shell/src/renderer/src/Home.tsx', `          {loggedIn ? (\n            <div className="account-menu-info">\n              <span className="account-menu-email" title={email}>\n                {email || t('loggedIn')}\n              </span>\n            </div>\n          ) : (\n            <>\n              <button\n                className="account-menu-item"\n                role="menuitem"\n                onClick={startLogin}\n                title={waiting ? t('waitingLogin') : undefined}\n              >\n                {waiting ? t('waitingShort') : t('loginGenspark')}\n              </button>\n              {waiting && authUrl && (\n                <>\n                  <button\n                    className="account-menu-item login-rescue"\n                    role="menuitem"\n                    onClick={openLoginUrl}\n                  >\n                    {t('loginOpenManually')}\n                  </button>\n                  <button\n                    className="account-menu-item login-rescue"\n                    role="menuitem"\n                    onClick={copyLoginUrl}\n                  >\n                    {urlCopied ? t('loginCopied') : t('loginCopyUrl')}\n                  </button>\n                </>\n              )}\n            </>\n          )}\n          <div className="account-menu-divider" />`, `          <div className="account-menu-info"><span className="account-menu-email">OpenRouter AI</span></div>\n          <button className="account-menu-item" role="menuitem" onClick={() => { setShowAiSettings(true); setMenuOpen(false) }}>AI Settings · OpenRouter</button>\n          <div className="account-menu-divider" />`)

// Remove login rescue hint entirely and make the visible account button an AI Settings button.
patch('apps/shell/src/renderer/src/Home.tsx', `      {!menuOpen && waiting && authUrl && (\n        <div className="login-hint" role="status">\n          <button className="login-hint-open" onClick={openLoginUrl}>\n            {t('loginOpenManually')}\n          </button>\n          <button className="login-hint-copy" onClick={copyLoginUrl}>\n            {urlCopied ? t('loginCopied') : t('loginCopyUrl')}\n          </button>\n        </div>\n      )}\n      <button\n        className="account-btn"\n        onClick={handleClick}`, `      <button\n        className="account-btn"\n        onClick={handleClick}`)
patch('apps/shell/src/renderer/src/Home.tsx', `        title={\n          loggedIn\n            ? email || t('loggedInGenspark')\n            : waiting\n              ? t('waitingLogin')\n              : (errorText ?? t('loginGenspark'))\n        }\n        aria-label={loggedIn ? t('account') : t('login')}`, `        title="OpenRouter AI Settings"\n        aria-label="OpenRouter AI Settings"`)
patch('apps/shell/src/renderer/src/Home.tsx', `        <span\n          className={\`account-avatar\${loggedIn ? ' logged-in' : ''}\${waiting ? ' waiting' : ''}\`}\n        >\n          {waiting ? (\n            <svg\n              className="account-spinner"\n              width="14"\n              height="14"\n              viewBox="0 0 16 16"\n              aria-hidden="true"\n            >\n              <circle\n                cx="8"\n                cy="8"\n                r="6"\n                stroke="currentColor"\n                strokeWidth="1.8"\n                fill="none"\n                strokeDasharray="26"\n                strokeDashoffset="18"\n                strokeLinecap="round"\n              />\n            </svg>\n          ) : (\n            initial\n          )}\n        </span>\n        <span className="account-text">\n          {loggedIn ? (\n            <>\n              <span className="account-name">{email ? email.split('@')[0] : t('loggedIn')}</span>\n              <span className="account-sub" title={email}>\n                {email || 'Genspark'}\n              </span>\n            </>\n          ) : (\n            <>\n              <span className="account-name">{waiting ? t('waitingShort') : t('login')}</span>\n              <span className={\`account-sub\${!waiting && errorText ? ' error' : ''}\`}>\n                {!waiting && errorText ? errorText : t('accountGenspark')}\n              </span>\n            </>\n          )}\n        </span>`, `        <span className="account-avatar logged-in">AI</span>\n        <span className="account-text"><span className="account-name">AI Settings</span><span className="account-sub">OpenRouter · Nemotron</span></span>`)

// Add the actual editable dialog to the working app.
patch('apps/shell/src/renderer/src/Home.tsx', `    </div>\n  )\n}\n\n// ── Cloud (Genspark web) projects view`, `      {showAiSettings && (\n        <div className="or-settings-overlay" onClick={() => setShowAiSettings(false)}>\n          <div className="or-settings-card" role="dialog" aria-modal="true" aria-label="OpenRouter AI Settings" onClick={(e) => e.stopPropagation()}>\n            <div className="or-settings-head"><div><h3>AI Settings</h3><p>Direct OpenRouter connection · no Genspark account required</p></div><button onClick={() => setShowAiSettings(false)} aria-label="Close">×</button></div>\n            <label>Provider<input value="OpenRouter" disabled /></label>\n            <label>OpenRouter API Key<input type="password" value={openRouterKey} placeholder="sk-or-v1-..." onChange={(e) => { setOpenRouterKey(e.target.value); setAiSaved(false) }} /></label>\n            <label>Model<select value={openRouterModel} onChange={(e) => { setOpenRouterModel(e.target.value); setAiSaved(false) }}><option value="nvidia/nemotron-3-ultra-550b-a55b:free">NVIDIA Nemotron 3 Ultra (Free)</option><option value="openrouter/free">OpenRouter Free Router</option></select></label>\n            <p className="or-settings-note">The API key is saved locally on this computer. It is not embedded in the installer or GitHub repository.</p>\n            <div className="or-settings-actions"><span>{aiSaved ? 'Saved ✓' : ''}</span><button onClick={() => void window.aiOffice.setOpenRouterSettings({ apiKey: openRouterKey, model: openRouterModel }).then(() => setAiSaved(true))}>Save AI Settings</button></div>\n          </div>\n        </div>\n      )}\n    </div>\n  )\n}\n\n// ── Cloud (Genspark web) projects view`)

const cssPath = 'apps/shell/src/renderer/src/home.css'
let css = readFileSync(cssPath, 'utf8')
if (!css.includes('.or-settings-overlay')) {
  css += `\n\n/* OpenRouter / Nemotron settings */\n.or-settings-overlay{position:fixed;inset:0;background:rgba(15,23,42,.28);display:flex;align-items:center;justify-content:center;z-index:10000;backdrop-filter:blur(2px)}\n.or-settings-card{width:min(460px,calc(100vw - 40px));background:#fff;border:1px solid #e5e7eb;border-radius:16px;box-shadow:0 24px 70px rgba(15,23,42,.22);padding:22px;color:#111827}\n.or-settings-head{display:flex;align-items:flex-start;justify-content:space-between;margin-bottom:18px}.or-settings-head h3{font-size:20px;margin:0 0 4px}.or-settings-head p{margin:0;color:#667085;font-size:13px}.or-settings-head button{border:0;background:transparent;font-size:25px;line-height:1;color:#667085;cursor:pointer}.or-settings-card label{display:block;font-size:12px;font-weight:600;color:#475467;margin:13px 0}.or-settings-card input,.or-settings-card select{display:block;width:100%;box-sizing:border-box;margin-top:6px;border:1px solid #d0d5dd;border-radius:9px;padding:10px 11px;background:#fff;color:#101828;font:inherit;outline:none}.or-settings-card input:focus,.or-settings-card select:focus{border-color:#667085;box-shadow:0 0 0 3px rgba(102,112,133,.12)}.or-settings-card input:disabled{background:#f9fafb;color:#667085}.or-settings-note{font-size:12px;line-height:1.5;color:#667085;margin:14px 0}.or-settings-actions{display:flex;align-items:center;justify-content:space-between;min-height:38px}.or-settings-actions span{font-size:12px;color:#067647}.or-settings-actions button{border:0;border-radius:9px;background:#101828;color:#fff;padding:10px 15px;font-weight:600;cursor:pointer}\n`
  writeFileSync(cssPath, css)
}

console.log('OpenRouter/Nemotron direct-AI patches applied; Genspark login UI removed.')
